<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE configuration  
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- 全局配置参数 -->
	<!-- <settings> </settings> -->

	<!-- 别名定义 -->
	<typeAliases>

		<!-- 批量别名定义 指定包名，mybatis自动扫描包中的po类，自定义别名，别名就是类名（首字母大小写都可以） -->
		<package name="wyz.jingangHotel02.ssm.autoPo" />

	</typeAliases>

</configuration>  